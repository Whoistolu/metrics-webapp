// import reducer, { continentReducer } from './continentReducer'

// describe('test the reducer function for the store', () => {
//   test('reducer returns new state', => {
//     const newState = [1,2,3];
//     const result = rducer(state: undefined, {type: undefined, payload: newState});
//     expect(result[0]).toBe(expected: 1);
//     expect(result[1]).toBe(expected: 2);
//     expect(result[2]).toBe(expected: 3);

//   })
// })
